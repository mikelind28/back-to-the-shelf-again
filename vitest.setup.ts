import "@testing-library/jest-dom";
// This gives you matchers like .toBeInTheDocument(), .toBeVisible(), .toHaveTextContent(), etc.
// It runs before every test file automatically (via setupFiles in vitest.config.ts)